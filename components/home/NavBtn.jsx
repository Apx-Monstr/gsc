const NavBtn = ({title})=>{
    return (
        <div>
            <button class = "p-4 py-2 rounded-full text-[#0f172a] hover:text-[#0f172a]">
            {title}
            </button>
        </div>
    )
}

export default NavBtn;